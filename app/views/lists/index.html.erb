<h1>Listes</h1>

<%= link_to "Créer une nouvelle liste", new_list_path, class: "btn btn-primary mb-4" %>

<% if @lists.any? %>
  <ul class="list-group">
    <% @lists.each do |list| %>
      <li class="list-group-item">
        <%= link_to list.name, list %>
        <%= link_to "Supprimer", list, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "btn btn-danger btn-sm" %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>Aucune liste disponible pour le moment.</p>
<% end %>
